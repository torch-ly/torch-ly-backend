import fs from "fs";
import {lastMap} from "../../file-handler";

//const mapDir = "maps/";

export let characters = [];

loadCharacters();

function loadCharacters() {
    //characters = fs.readFileSync(lastMap).characters;
}

